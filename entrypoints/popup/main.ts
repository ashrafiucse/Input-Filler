// Popup logic is expanded in a later unit (Fill all / Fill this form / Clear
// buttons + fill-count footer). This stub proves the popup entrypoint builds.
const app = document.getElementById('app');
if (app) app.textContent = 'Input Filler';

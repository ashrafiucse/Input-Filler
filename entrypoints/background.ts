export default defineBackground(() => {
  // Expanded in a later unit: toolbar-popup messaging, keyboard commands, and
  // the context-menu fill/clear items. This stub proves the background builds.
  console.log('[input-filler] background loaded');
});

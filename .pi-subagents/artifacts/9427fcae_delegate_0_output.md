My targeted searches confirm all three findings. Verified:
- Only **KD1–KD4** exist (lines 79–82); line 334 cites `KD7`, which does not resolve — the matching decision is **KTD7** (rules compile/cache/first-match/urlPattern).
- **U9 (Popup)** is the only behavior-bearing unit with Test scenarios + a Verification field but **no `tests/*.test.ts`** in its Files, and — unlike U12 (line 447) — it documents **no no-test exception**, so it doesn't clearly satisfy the DoD per-unit clause (line 471).
- **Safari** is listed in both deferred sections: line 95 (v2+ scope) and line 111 (Follow-Up Work / environment-blocked).

{"reviewer":"technical-editor-internal-consistency","findings":[{"title":"U6 cites nonexistent KD7 decision","severity":"P2","section":"Implementation Units / U6 Custom Field Rules engine (Product Contract provenance link)","why_it_matters":"A reader chasing U6's governance reference finds nothing: only KD1–KD4 are defined, so 'KD7' is an unresolvable cross-reference that breaks the unit's decision-provenance trace. The decision it intends — compile-to-regex-once, cache, first-match-wins, urlPattern scoping — is KTD7, which directly governs the rules engine. Renaming to KTD7 restores the traceable link.","finding_type":"error","autofix_class":"safe_auto","suggested_fix":"Change U6's 'Governs KD3, KD7.' to 'Governs KD3, KTD7.'","confidence":100,"evidence":["Requirements:** R16, R17. Governs KD3, KD7.","- KD4. v1 settings ship the practical core ... Governs R15, R12.","KTD7. **Custom rules compile to regex once and cache; first-match-wins in priority order; `urlPattern` scopes per-site.**"]},{"title":"U9 popup unit lacks test or exception","severity":"P2","section":"Implementation Units / U9 Popup UI vs Definition of Done (completion contract)","why_it_matters":"The DoD per-unit clause requires each unit's Test scenarios to pass OR a documented no-test exception. U9 lists behavioral Test scenarios and a Verification field, yet its Files omit any tests/*.test.ts (unlike U8 background and U10 rules-ui) and, unlike U12, it states no no-test exception — so a reviewer cannot tell whether U9 is tested, leaving its 'done' status ambiguous against the completion contract. Adding a popup test file (mirroring U10) or an explicit no-test/runtime note resolves it.","finding_type":"omission","autofix_class":"manual","suggested_fix":"Add `tests/popup.test.ts` to U9's Files and Verification (mirroring U10's rules-ui.test.ts for message-contract/theme logic), or add an explicit no-test/runtime-verification note as U12 does.","confidence":75,"evidence":["**Files:** `entrypoints/popup/index.html`, `entrypoints/popup/main.ts`, `entrypoints/popup/style.css`.","Per unit: that unit's `Verification` field is satisfied and its `Test scenarios` pass (or the documented no-test exception applies).","Test expectation: none — packaging/assets/config; verified by build and zip output."]}],"residual_risks":["Internal-consistency review only; feasibility of WXT/MV3 mechanics (KTD1/KTD3), regex-string generation correctness (U6), and the agree-to-terms-vs-checkbox 'toggle' interaction in the U4 fill flow are owned by feasibility/test personas and were not assessed here.","The KD7→KTD7 fix rests on intent inference (no KD5/6/7 exist; KTD7 is the only decision numbered 7); confirmed by enumeration, not by an author statement."],"deferred_questions":["Are unit 'Governs' fields intended to cite KTDs as well as KDs? This determines whether U6's fix is 'KTD7' vs. dropping the stray reference.","Is U9 popup verification intended as automated (jsdom + messaging mocks) or manual/runtime? Determines whether the fix is a new test file or a no-test exception note.","Should Safari appear in only one deferred section, or is the dual listing (v2+ scope vs. environment-blocked follow-up) intentional?"]}
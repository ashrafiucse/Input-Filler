All key evidence confirmed against source. Producing the review.

{"reviewer":"systems-architect","findings":[{"title":"Content injection model contradicts WXT entrypoint convention","severity":"P1","section":"Planning Contract (KTD3) / Units Contract (U7, U8, U9)","why_it_matters":"An implementer cannot tell whether content.ts is a static script or injected on demand: KTD3 says 'Content is injected via `activeTab`/`scripting` on user action', yet U7 ships `entrypoints/content.ts`, which WXT registers as a static content script that listens for messages. The unresolved choice changes permission usage, the popup→background→content message contract for U7/U8/U9, and whether `<all_urls>` plus activeTab is the right model. Declaring the static entrypoint (as the file structure implies) and removing the on-demand `executeScript` claim lets all three messaging units start consistently.","finding_type":"error","autofix_class":"gated_auto","suggested_fix":"Declare `entrypoints/content.ts` as a static WXT content script (matching the entrypoint file structure) that the background messages; remove the on-demand `scripting.executeScript` injection claim from KTD3 and reconcile the permissions block and sequence diagram to that model.","confidence":75,"evidence":["Content is injected via `activeTab`/`scripting` on user action (origin §3, §8).","U7 Files: `entrypoints/content.ts`, `entrypoints/content.style.css`, `tests/content.test.ts`.","B->>C: { action: fillAll }"]},{"title":"Popup suppresses action.onClicked; R1 trigger unresolved","severity":"P1","section":"Units Contract (U8) / Product Contract (R1)","why_it_matters":"With `entrypoints/popup/` declared, MV3 does not deliver `chrome.action.onClicked` — the toolbar icon opens the popup — so U8's premise 'On action click... message the active tab's content script' and the sequence diagram's 'toolbar click → background' describe a path that never fires. This forces the implementer to decide whether R1's 'via toolbar button' means instant icon-click fill (impossible with a popup) or fill-via-popup-button, a decision the plan should have made. Routing the toolbar trigger through the popup and dropping the direct action-click premise makes the trigger model match MV3.","finding_type":"error","autofix_class":"gated_auto","suggested_fix":"State that the toolbar icon opens the popup and fill is initiated from the popup buttons (popup → background → content); remove U8's 'on action click' direct-fill premise and the 'toolbar click → background' arrow in the sequence diagram.","confidence":75,"evidence":["On action click and `fill-all`/`fill-form` commands, read settings, check the active origin against `ignoredDomains` ... and message the active tab's content script.","U->>B: toolbar click / shortcut / menu","R1. The extension fills every fillable form field on the active tab on demand via toolbar button, keyboard shortcut, and (when enabled) context menu.","U9: Render Fill all forms, Fill this form, and Clear buttons"]},{"title":"Regex-string generation is an undeclared hard dependency","severity":"P1","section":"Units Contract (U6) / Product Contract (R16)","why_it_matters":"The `regex` fill kind must emit a random string matching arbitrary user-supplied regex (R16), but U6 only says 'regex (generate a matching string)' and names no dependency or supported subset. Generating valid output for anchors, character classes, `{n,m}` quantifiers, and alternation is a known-hard problem a hand-rolled generator will mishandle, yielding invalid values or hanging on pathological patterns — breaking R16 correctness for the engine the plan calls 'the valuable core' (KD3). Declaring a library like `randexp` makes the feature deliverable as specified.","finding_type":"omission","autofix_class":"gated_auto","suggested_fix":"Add a regex-string-generation dependency (e.g., `randexp`) to U6's dependency list and use it for the `regex` fill kind; document that user-supplied regex is generated via the library, not a hand-rolled generator.","confidence":75,"evidence":["Implement fill resolvers: `builtin` (delegate to generators), `static` (literal), `template` (resolve `{placeholders}` against generators), `list` (random entry), `regex` (generate a matching string).","regex `ORD-\\d{6}` generates a matching string; a pattern with a quantifier produces a valid length.","origin §5.1: regex → value = regex; a random matching string is generated"]},{"title":"Sync storage per-item quota breaks rule persistence","severity":"P2","section":"Units Contract (U6) / Product Contract (R17)","why_it_matters":"`browser.storage.sync` caps each key at 8,192 bytes (QUOTA_BYTES_PER_ITEM), but rules persist under sync and KTD7/origin §5.6 target 'hundreds of rules' — a single `rules` key overflows long before that, and `set()` then fails with `runtime.lastError` so rules silently stop persisting. The plan's generic 'local fallback' (KTD8) is specified for sync-unavailability, not per-item quota overflow. Chunking rules across multiple sync keys preserves R17 persistence at the cited scale.","finding_type":"omission","autofix_class":"gated_auto","suggested_fix":"Chunk custom rules across multiple `browser.storage.sync` keys (each under the 8KB per-item limit) so rule sets at the 'hundreds of rules' scale KTD7 targets still sync and persist; add a quota-overflow test scenario to U6.","confidence":75,"evidence":["Compilation happens when rules are loaded, so hundreds of rules add negligible per-fill latency","origin §5.6: Rules persist in `browser.storage.sync` ... with a local fallback. ... even hundreds of rules add negligible fill latency.","KTD8. Versioned settings in `browser.storage.sync` with a local fallback and a load-time migrator."]},{"title":"fillCurrentForm undefined when focused field has no form","severity":"P2","section":"Units Contract (U7) / Product Contract (R2)","why_it_matters":"Modern SPAs commonly render inputs without a wrapping `<form>`, but `fillCurrentForm(activeElement)` is defined only to 'scope to the owning `<form>`' (R2/U7) with no stated behavior when the focused field owns no form. On those pages 'Fill this form' (R2) silently no-ops or errors — a frequent real-world hit, not an edge case. Defining a no-`<form>` fallback scope makes R2 deliverable on formless layouts.","finding_type":"omission","autofix_class":"gated_auto","suggested_fix":"Define `fillCurrentForm`'s scope when the focused field has no owning `<form>` (e.g., nearest semantic container or the field's logical group) and add a no-`<form>` test scenario to U7.","confidence":75,"evidence":["fillCurrentForm(activeElement) (scope to the owning `<form>`)","R2. The extension fills only the `<form>` containing the currently focused field on demand","Happy path: `fillCurrentForm` fills only the focused field's form; other forms on the page are untouched."}],"residual_risks":["Live browser and SPA-runtime behavior (event-dispatch correctness, framework state sync) cannot be exercised in this build environment; manual QA is deferred, so R10/R11 runtime correctness is assumed, not proven.","WXT/MV3 API conventions are assumed from the origin; with no web access at planning time, scaffolding (entrypoint shape, commands registration, manifest fields) may need adjustment during U1/U11.","Restricted-origin pages (chrome://, about:, the Web Store, AMO) reject content injection; the plan's 'fails gracefully' directive covers this but cannot be verified without a browser, so uncaught errors on those pages remain possible.","Corpus license provenance for each hand-curated theme bank must be recorded; a provenance mistake risks bundling non-license-clean text (KD2)."],"deferred_questions":["Should the toolbar icon instantly fill (no default_popup) or always open the popup first? R1's 'via toolbar button' deliverability depends on this.","For fillCurrentForm, what scope should apply when the focused field has no owning <form>?","Is a regex-generation library (e.g., randexp) acceptable, or should the supported regex subset be explicitly bounded for the rules engine?","Should custom rules persist in storage.sync (chunked) or storage.local to avoid the 8KB per-item quota at scale?"]}
{"reviewer":"right-sizing","findings":[{"title":"Page-level name/email consistency untested at orchestrator","severity":"P2","section":"Implementation Units / U7 Content orchestration","why_it_matters":"Origin §11 demands 'Reproducible per field within a page so a name used in the name field matches the one in the email field.' U2 tests only single-call consistency ('fullName and email stay consistent'); U7's integration scenarios never assert cross-field reuse. Concrete consequence: a multi-field form could render 'Eleanor Whitfield' beside 'john.smith@mail.com', yielding incoherent screenshots that defeat origin §11's stated purpose. Fix: add a U7 integration scenario asserting the email local-part derives from the page's chosen name.","finding_type":"omission","autofix_class":"gated_auto","suggested_fix":"Add to U7 Test scenarios: 'Happy path: across a multi-field form, the generated email local-part derives from the same name used in the name field (origin §11 page-level consistency).'","confidence":50,"evidence":["Origin §11: 'Reproducible per field within a page so a name used in the name field matches the one in the email field (e.g. jane.doe@...).'","U2 Test scenarios: 'Happy path: fullName and email stay consistent (the email local-part derives from that name).' — single-call only","U7 Test scenarios (mixed form, fillCurrentForm, clearAllForms, AE1-AE3, ignoreFields, ignoreFieldsWithContent) list no cross-field/page-level consistency assertion"]},{"title":"env.ts module likely speculative under KD1","severity":"P3","section":"Implementation Units / U5 (and High-Level Technical Design)","why_it_matters":"The right-sizing test is whether each module earns multiple current consumers. env.ts ('per-browser capability flags') shows no consumer anywhere in the design, and KD1 narrows v1 to Chromium + Firefox while WXT abstracts every divergence origin §0 enumerates (namespace, background, action vs browser_action, contextMenus, commands). With no runtime divergence remaining, env.ts risks shipping as a near-empty module that does not earn its keep. Fix: document a concrete v1 consumer or fold any real flag into settings.ts.","finding_type":"omission","autofix_class":"gated_auto","suggested_fix":"Either document env.ts's concrete v1 consumers (which capability flags actually diverge after KD1 + WXT) or fold its flags into settings.ts and remove the standalone module.","confidence":50,"evidence":["U5 Files: 'src/lib/settings.ts, src/lib/env.ts' — env.ts listed but no consumer referenced in any other unit or the flow/sequence diagrams","KD1: 'v1 browser scope is Chromium + Firefox only; Safari is deferred.' (session-settled)","KTD1: WXT 'auto-generates per-browser manifests' and 'polyfills the promise-based browser namespace,' covering the divergences origin §0 lists"]},{"title":"Filler/generator value ownership unclear for constrained types","severity":"P3","section":"Implementation Units / U4 Per-type fillers","why_it_matters":"U4 declares a dependency on U2 and states 'range/number respect min/max/step; date/color set valid values,' yet fillField's signature is (field, type, value) and the design flow shows generation (GN) before fill (FL), implying fillField receives a pre-made value. It is unspecified whether constrained-type values are generated by the orchestrator (U7) or inside fillers (U4); both paths being implied risks duplicated or conflicting logic for number/range/date/color/select. Fix: name one owner explicitly.","finding_type":"omission","autofix_class":"gated_auto","suggested_fix":"Specify that U7 extracts field constraints and generates all values via generators while fillField only applies a value + dispatches events (drop U4→U2); OR state that fillers own constrained-type generation and import generators.","confidence":50,"evidence":["U4 Depends on 'U2'; Approach: 'fillField(field, type, value) ... range/number respect min/max/step; date/color set valid values.'","Design flowchart: 'DT → GN → FL' and 'RV → GN → FL' (generation precedes fill, implying fillField consumes a value)","U7 Approach: 'generate, apply confirmation-field copy and agree-to-terms overrides, then fillField' — generation attributed to the orchestrator"]}],"residual_risks":["Auto-theme detection (origin §4.1 'auto-detect guesses from context — a comment/message field → support, a review field → reviews') is not assigned to any unit and is absent from the Deferred list; v1 silently relies on default theme + per-rule pin. This is a silent scope reduction (coherence-adjacent), not right-sizing over-reach.","No shared message-contract module exists; background/content/popup (U7/U8/U9) each re-derive the fill/clear message shape, risking contract drift across three consumers.","text.ts (dummyText + corpus/grammar engine) is a single-consumer module split out from origin's generators.ts (origin §2 file tree lists dummyText under generators); the split is defensible by concern separation but its single consumer (content orchestrator) leaves it borderline.","jsdom DOM-integration tests cannot prove real React/Vue/Angular state sync for R10/R11; the only true proof (live browser load) is deferred and cannot run in this build environment (acknowledged in Verification Contract).","Corpus license provenance for the six hand-curated theme banks (general/business/tech/casual/support/reviews) is unverified until implementation; KD2 settled the approach but the 'unambiguously license-clean' guarantee requires per-bank provenance records (Open Question)."],"deferred_questions":["Should env.ts remain a standalone module given KD1 narrows runtime browser divergence to near-zero, or should its flags fold into settings.ts / wxt.config.ts?","For constrained input types (number/range/date/color), should value generation live in the U7 orchestrator or the U4 fillers — the GN→FL flow and the U4→U2 dependency imply different owners.","Should page-level name/email consistency (origin §11) be verified at U7 (cross-field within a page), not only at U2 (single-call)?","Is a shared message-types module warranted given background/content/popup (U7/U8/U9) all consume the same fill/clear contract?"]}